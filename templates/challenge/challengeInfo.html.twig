{% extends('base.html.twig') %}

{% block body %}
    {{ challenge.title }}</br>
    {{ challenge.description }}</br>
    {{ challenge.category }}</br>
    {{ challenge.subcategory }}</br>
    {{ challenge.points }}</br>
    {{ challenge.duration }}
{% endblock %}
