{% extends('base.html.twig') %}

{% block body %}
<table style="width:100%">
    {% for challenge in challenges %}
    <tr>
        <td><a href="{{path('showChallenge', {uuid: challenge.uuid})}}">{{ challenge.title }}</a></td>
    </tr>
    {% endfor %}
</table>
{% endblock %}
